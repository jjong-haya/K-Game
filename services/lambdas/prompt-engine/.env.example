# AWS region for Bedrock / Lambda IAM invoke
AWS_REGION=us-east-1

# Bedrock model ID
BEDROCK_MODEL_ID=amazon.nova-lite-v1:0

# Optional Gemini fallback inside the Lambda
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
